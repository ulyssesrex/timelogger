var button = $('#timelog-button');

button.html("<%= j(render partial: 'shared/end_timelog_button') %>");
button.append("<br>");
button.append("<%= j(render partial: 'shared/cancel_timelog_button') %>");

 
activateTimer();