$('.grants-allocations')
	.fadeToggle('fast')
	.html("<%= j( 
		render partial: 'users/grantholding', 
		collection: @grantholdings, 
		locals: { since_date: @since_date }) 
	%>")
	.fadeToggle('fast');