<% provide(:title, "Confer admin status") %>

<h2>Confer admin status</h2>

<% unless @users.empty? %>

	<%= form_tag make_admin_path, method: :put do %>

		<table class="table table-striped table-hover ">
		  <thead>
		    <tr>
		      <th>Name</th>
		      <th>Position</th>
		      <th>Admin</th>
		    </tr>
		  </thead>
		  <tbody>
		    <% @users.each do |user| %>
		      <tr>
		        <td><%= full_name(user) %></td>
		        <td><%= user.position %></td>
		        <td>
		        	<div class="checkbox">
		          	<%= check_box_tag 'user_ids[]', user.id, user.admin? %>
		        	</div>
		      	</td>
		      </tr>
		    <% end %>
		  </tbody>
		</table>

		<br><br>

		<div class="form-group">
		  <div class="col-lg-10 col-lg-offset-2">
		    <%= link_to "Cancel", user_path(current_user), class: "btn btn-default" %>
		    <%= submit_tag "Submit changes", class: "btn btn-primary" %>
		  </div>
		</div>
	<% end %>

<% else %>

	<br>
	<p>(No other users yet)</p>
	<br>
	<p><%= link_to 'Back to home', user_path(current_user) %></p>

<% end %>


